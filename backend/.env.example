# FastAPI Job Search Web Application Environment Variables

# Application Settings
DEBUG=true
PROJECT_NAME="JobSpy Web API"
VERSION="1.0.0"

# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS Settings (comma-separated list)
ALLOWED_HOSTS=http://localhost:3000,http://127.0.0.1:3000

# JobSpy Configuration
DEFAULT_RESULTS_WANTED=20
MAX_RESULTS_WANTED=100
DEFAULT_DISTANCE=50
MAX_DISTANCE=200

# Cache Settings
CACHE_TTL_SECONDS=300